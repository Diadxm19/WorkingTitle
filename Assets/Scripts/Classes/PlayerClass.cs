using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerClass : BaseStats
{
    // Start is called before the first frame update
    void Start()
    {
        SetHealth(100);
        SetSpeed(10.0f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
